Промпт ТЗ для Replit Agent: Frontend для мобильного веб-приложения "Natali Secrets"

Общие требования
Технологии: React + TypeScript (функциональные компоненты, хуки).

Целевая аудитория: унисекс-дизайн (удобен для мужчин и женщин).

Цветовая палитра (HEX):

Сиреневый: #9D7AD9

Серебристый: #C0C0C0

Белый: #FFFFFF

Мокрый асфальт: #505050

Тёмно-оранжевый: #FF6B35 (акцентные элементы).

Адаптивность: Mobile-first, поддержка планшетов и Desktop (медиазапросы, Grid/Flex).

Язык: Русский (проверить локализацию всех элементов).

Компоненты интерфейса
1. Навигационная панель
Расположение: Фиксированный bottom bar (на всех страницах).

Иконки + текст:

Главная (🏠)

Акции (🎁)

Услуги (💅)

Настройки (⚙️)

О нас (ℹ️)

Адаптация:

На десктопе → горизонтальное меню внизу экрана.

На мобильных → классический bottom bar (5 иконок с подписями).

Интерактив: Анимация при нажатии, подсветка активной вкладки.

2. Главная страница
Верхний блок:

Прямоугольная карточка (горизонтальный скролл) с популярными услугами:

Заголовок: "Популярные услуги".

Изображение услуги + название + цена.

Стиль: Плоский дизайн, фон #FFFFFF, тень 0px 4px 12px rgba(0,0,0,0.1).

Нижний блок:

4 квадратные мини-карточки "Комплексы" (2x2 Grid):

Примеры: "Уход за кожей", "Мужской уход", "SPA-программы".

Дизайн: Фон #9D7AD9, белый текст, иконка в центре.

3. Страница "Акции"
Вертикальный список карточек с акциями:

Формат: Изображение + заголовок + краткое описание + срок действия.

Стиль: Фон #FFFFFF, акцентные кнопки #FF6B35.

4. Страница "Услуги"
Фильтры: Выпадающие списки по категориям (например, "Лицо", "Тело", "Мужчинам").

Сетка услуг:

Компактные карточки (на мобильных — 1 колонка, на десктопе — 3 колонки).

Элементы: Название, цена, кнопка "Подробнее" (#FF6B35).

При нажатии → модальное окно с описанием услуги.

5. Дополнительные страницы
Настройки: Переключатель уведомлений, выбор языка, кнопка "Связаться с поддержкой".

О нас:

Логотип студии.

Контакты (адрес, телефон, ссылка на сайт).

Карта с локацией.

Технические детали
Адаптивная сетка:

Использовать CSS Grid для компоновки.

Брейкпоинты: 320px (mobile), 768px (tablet), 1024px (desktop).

Состояние:

Управление навигацией через React Router.

Данные услуг/акций — моковый JSON (для теста).

Оптимизация:

Ленивая загрузка изображений (Intersection Observer).

Минификация CSS/JS.